# metadev

Runs on io.js.


```bash
# SETUP
$ git clone https://github.com/metagen/metadev && cd metadev
$ npm install

# SERVE IN TERMINAL
./bin/serve.js

# DEVELOP
npm install -g nodemon
nodemon ./bin/serve.js
```
